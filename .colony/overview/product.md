# Product Overview

Build a very small Pomodoro timer web app.

Requirements:

- Use DaisyUI classes for the interface.
- Use plain HTML, CSS, and JavaScript rather than a heavy framework.
- Play real ticking sounds while the timer is running.
- Play a completion sound when the session ends.
- Keep the implementation intentionally lightweight so it is cheap for agents to
  build and easy to validate repeatedly.
