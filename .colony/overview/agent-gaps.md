# Agent Gaps

Known implementation agents already exist in `agents/`.

- `frontend-specialist` can build the browser UI and audio behavior.
- `generalist` can handle lightweight implementation and regression testing.

Do not create new implementation agents unless the existing agents are proven
insufficient for the assigned work.
